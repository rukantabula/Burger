class ReactVitalsService {

    sendMetrics(metrics: string) {
        // Send vitals to backend or elastic
    }
}

const reactVitalsService: ReactVitalsService = new ReactVitalsService();
export default reactVitalsService;